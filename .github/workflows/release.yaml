---
name: Build Release

on:
  push:
    tags:
      - '*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Composer validate
        run: composer validate --strict

      - name: Composer install
        run: composer install --no-dev --optimize-autoloader

# These may be required after merging the mix -> vite pr
#     - name: Node clean install
#       run: npm ci
#     - name: Node build
#       run: npm run build

      # used by ansible to skip deployment
      - name: Create version file
        run: echo '${{ github.ref_name }}' > VERSION

      - name: Compress artifacts
        run: >
          tar czf moon-mining-manager-${{ github.ref_name }}.tar.gz
          app/
          bootstrap/
          config/
          database/
          public/
          resources/
          routes/
          storage/
          vendor/
          .env.example
          LICENSE
          README.md
          VERSION
          artisan
          composer.json

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          files: moon-mining-manager-${{ github.ref_name }}.tar.gz
          prerelease: ${{ contains(github.ref_name, 'test') }}
          generate_release_notes: ${{ !contains(github.ref_name, 'test') }}
